/**
 * Vehicle is the interface to get information about the traffic dependent on its vehicles.
 */
public interface Vehicle {
    /**
     * Returns the vehicle's identifier.
     *
     * @return identifier of the vehicle
     */
    int getId();
}
